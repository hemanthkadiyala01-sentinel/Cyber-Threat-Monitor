import PagePlaceholder from "../components/ui/PagePlaceholder";

export default function AssetsPage() {
  return (
    <PagePlaceholder
      title="Asset Inventory"
      description="View monitored endpoints, servers, and cloud workloads under continuous SOC visibility."
      highlights={["Scan new host", "Tag critical asset", "View asset posture", "Export inventory"]}
    />
  );
}
